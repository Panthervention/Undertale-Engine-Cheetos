//if (surface_exists(surface_clip))
//	surface_free(surface_clip);

if (surface_exists(surface_mask))
	surface_free(surface_mask);

if (surface_exists(__surface_frame))
	surface_free(__surface_frame);
